# 面试官：你是怎么处理死循环的？

- 怎么判断的：
  当某一个工具函数被重复调用，传入的参数一致，执行得到的结果也是一致，并且循环的次数达到了一定的次数时，并且没有任何进展，我则认为它陷入了一个死循环，我在项目中设置的最大循环次数为15次，当次数达到最大循环次数及以上时则说明这是一个死循环。

- 怎么应对的：
  我借助了Hash算法，将调用到的工具函数名，传入的参数以及执行结果处理成一个唯一的Hash值作为工具指纹，并且存放进一个数组，通过滑动窗口判断最近30次中工具调用的状态，当同一工具指纹出现了5次，我就给LLM注入了一个警告提示，“当前可能进入死循环，请更换其他方式解决问题”，如果次数达到了15次及以上，我就将这个循环直接熔断。

# 面试官：你是怎么处理API容错的？

我先通过将错误分类，规划确认哪些错误是需要做重试机制的，哪些错误是不需要重试机制的，例如状态码为429，408.500-600之间的需要重试，状态码在400-500之间的不需要重试，在重试机制中，我对重试的时间做了指数退避 + 随机抖动策略，并且我在项目中设置了最大重试次数为3次，当重试次数到达3次及以上，我原本打算做模型降级，但是考虑到模型降级的性能问题并且项目还在迭代中，所以我并没有在项目中做这一操作，而是直接返回错误信息，停止重试。

# 在你的Agent系统中，是怎么处理工具的并发问题的？

- 工具打造时:
  我在定义工具时，设置了一个变量判断工具能否与其他工具并行。例如：`read_file`、`list_directory` 这类只读工具标注 可并行，而 `write_file` 这类会修改状态的工具标注 不可并行。

- 等到AI SDK 触发工具时:
  当AI SDK 触发工具时，我让工具通过这个变量判断工具能否并行去获取对应锁。我设置了三个变量`exclusiveLock`（是否有独占锁持有者）、`concurrentCount`（当前共享锁持有数）、`waitQueue`（阻塞等待队列），来构成一把读写锁。可并行的工具去获取共享锁，并且记录共享锁持有者数，只要没有独占锁就可以并发持有执行；不可并发的工具去获取独占锁，通过设置的变量判断是否有独占锁持有者或者共享锁持有者，如果有，就阻塞后续所有的锁请求，无法执行，必须等所有共享锁和独占锁都释放。锁释放后再将等待队列里所有 `resolve` 全部唤醒，让它们重新去抢锁，执行后在 `finally` 里释放锁。

# 面试官: 你接入了哪些第三方的工具？怎么接入的？

- githubMCP: 我去魔搭平台，在MCP广场查看了一些通用的MCP服务有哪些，我打造的Agent主要是用于Code方面，所以我选择了githubMCP服务。

- MCP客户端: 我用node.js自己实现了一个MCP Client，通过spawn启动一个子进程去连接gitHub的MCP Server，实现gitHub MCP Server 的工具调用，并将调用结果返回给父进程。

# 面试官: 工具太多大模型选不准，你是怎么处理的?

- 为什么会选不准: 工具太多，上下文压力增加，挤占了真正用于推理的上下文空间，LLM 的注意力会下降,并且内置的工具和用户接入的工具可能会有语义重叠，导致模型在选择工具时容易混淆，误选，漏选，导致 LLM 选不准。

- 怎么处理的:(ToolSearch)
  我用"延迟加载 + 关键词检索"。核心工具：全量携带在单次上下文中，低频工具：不直接加载到上下文，而是通过 ToolSearch 元工具进行延迟加载，工具注册时给需要延迟的工具、MCP工具添加 shouldDefer 和 searchHint 两个字段，每轮根据用户 query ，然后通过 ToolSearch 元工具匹配 searchHint 把相关工具激活,状态由延迟设置为活跃，再和核心工具一起塞给模型。这样 LLM 每轮看到的工具不会过多，避免了上下文爆炸和语义重叠，提高了调用工具准确率。

# 面试官: 你是怎么处理Agent的上下文记忆的？

- 我看过openClaw 和 ClaudeCode 各自的方案:
  - openClaw: openClaw使用的方案是将上下文记忆存储在SOLite数据库中
  - ClaudeCode:ClaudeCode使用的方案是将每次的上下文记忆都存储进JSON文件中

- 我的方案是:我的方案是学习的ClaudeCode采用的方案，我将每次的上下文记忆都存储进JSONL格式的文件
  - 为什么: JSONL格式简单、易读、易写
    1. 不怕崩溃，最多丢失最后一条数据
    2. 方便调试，可以直接人为打开文件，查看数据
    3. 零依赖，不需要安装任何库

# 面试官: 上下文过长，你是怎么处理的?

- 上下文中包含了: 上下文中包含了 system prompt、用户输入、工具调用结果和历史对话记录，而其中导致上下文过长的主要是工具调用结果和历史对话记录

- 怎么处理的: 我先对上下文进行了紧凑化压缩，将工具调用结果一换成了一句占位符，并且设置了白名单，只有白名单里的工具可清理（比如读文件、写文件、搜索等工具），以外的一律保留，并且设置了需要保留最近3条工具调用结果，然后我还进行了摘要化处理，先估算上下文token，如果token小于我设置的阈值或者最近消息数小于6条，不进行摘要处理，否则就要摘要，我设置了一套标准的摘要生成模板，通过新开一个会话去让大模型按照这个模板将历史记录进行摘要，然后再将摘要返回

- 我先进行紧凑化再进行摘要是因为，紧凑化成本低并且不会有语义丢失，所以先试用它，而摘要有API成本并且可能会导致信息丢失，所以它只在达到阈值后才触发

# 面试官: 摘要压缩每次调用大模型也是需要成本的，你是怎么处理的？
- 三层防线:

- 摘要:
